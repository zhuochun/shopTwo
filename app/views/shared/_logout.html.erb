<ul class="nav navbar-nav navbar-right">
  <%= content_tag(:li, render("shared/menu_cart")) unless current_user.management? %>
  <li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
      <i class="glyphicon glyphicon-<%= current_user.role %>"></i>
      <%= current_user.username %> <b class="caret"></b></a>
    <ul class="dropdown-menu">
      <li><%= link_to "Profile", edit_user_registration_path, class: "navbar-link" %></li>
      <%= content_tag(:li, link_to("Your Orders", orders_path, class: "navbar-link")) unless current_user.management? %>
      <li class="divider"></li>
      <li><%= link_to "Logout", destroy_user_session_path, method: :delete, class: "navbar-link" %></li>
    </ul>
  </li>
</ul>
